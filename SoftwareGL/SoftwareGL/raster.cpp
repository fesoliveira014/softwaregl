#include "raster.hpp"

namespace raster
{
    /*void line(int x0, int y0, int x1, int y1, TGAImage &image, TGAColor color)
    {
        for (float t = 0.f; t < 1.f; t += 0.01f) {
            uint x = x0 * (1. - t) + x1 * t;
            uint y = y0 * (1. - t) + y1 * t;
            image.Set(x, y, color);
        }
    }*/
}